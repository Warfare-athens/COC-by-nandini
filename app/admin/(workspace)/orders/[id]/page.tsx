import { notFound } from "next/navigation";
import AdminOrderDetailForm, { WhatsAppOrderButton } from "@/app/components/AdminOrderDetailForm";
import { getSupabaseAdmin } from "@/db";

type Row=Record<string,unknown>; const value=(v:unknown)=>String(v??"—"); const money=(v:unknown)=>`₹${Number(v||0).toLocaleString("en-IN")}`;
export default async function AdminOrderPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const{data}=await getSupabaseAdmin().from("orders").select("*,addresses:shipping_address_id(*),order_items(*),fulfillments(*,tracking_events(*)),payments(*)").eq("id",id).maybeSingle();if(!data)notFound();const order=data as unknown as Row;const address=(order.addresses||{})as Row;const items=(order.order_items||[])as Row[];const fulfillments=(order.fulfillments||[])as Row[];const fulfillment=fulfillments[0]||{};const events=(fulfillment.tracking_events||[])as Row[];return <><div className="admin-head"><div><h1>{value(order.order_number)}</h1><p>{value(order.email)} · {value(order.phone)}</p></div><div style={{display:"flex",gap:"10px",alignItems:"center"}}><WhatsAppOrderButton phone={value(order.phone)} orderNumber={value(order.order_number)} customerName={value(address.full_name)} totalInr={value(order.total_inr)}/><a className="admin-button" href={`/admin/invoices/${id}`} target="_blank">Invoice</a></div></div><div className="admin-detail-grid"><section><div className="admin-panel" style={{margin:0}}><div className="admin-panel-head"><h2>Items</h2></div><table className="admin-table"><thead><tr><th>Product</th><th>SKU</th><th>Qty</th><th>Total</th></tr></thead><tbody>{items.map(item=><tr key={value(item.id)}><td><b>{value(item.product_name)}</b><small className="admin-row-sub">{value(item.variant_title)}</small></td><td>{value(item.sku)}</td><td>{value(item.quantity)}</td><td>{money(item.total_inr)}</td></tr>)}</tbody></table></div><div className="admin-panel"><div className="admin-panel-head"><h2>Update order & tracking</h2></div><AdminOrderDetailForm order={{id,status:value(order.status),fulfillment_status:value(order.fulfillment_status),carrier:value(fulfillment.carrier)==="—"?null:value(fulfillment.carrier),tracking_number:value(fulfillment.tracking_number)==="—"?null:value(fulfillment.tracking_number),tracking_url:value(fulfillment.tracking_url)==="—"?null:value(fulfillment.tracking_url)}}/></div><div className="admin-panel"><div className="admin-panel-head"><h2>Tracking timeline</h2></div>{events.length?<div className="admin-summary-list">{events.map(event=><div key={value(event.id)}><span><b>{value(event.status)}</b> · {value(event.message)}</span><small>{new Date(value(event.occurred_at)).toLocaleString("en-IN")}</small></div>)}</div>:<div className="admin-empty">No tracking events.</div>}</div></section><aside><div className="admin-panel" style={{margin:0}}><div className="admin-panel-head"><h2>Summary</h2></div><div className="admin-summary-list"><div><span>Subtotal</span><b>{money(order.subtotal_inr)}</b></div><div><span>Discount</span><b>{money(order.discount_inr)}</b></div><div><span>Shipping</span><b>{money(order.shipping_inr)}</b></div><div><span>Tax</span><b>{money(order.tax_inr)}</b></div><div><span>Total</span><b>{money(order.total_inr)}</b></div><div><span>Payment</span><b>{value(order.payment_status)}</b></div><div><span>Method</span><b>{value(order.payment_method)}</b></div></div></div><div className="admin-panel"><div className="admin-panel-head"><h2>Delivery address</h2></div><div className="admin-summary-list"><p>{value(address.full_name)}<br/>{value(address.line1)} {value(address.line2)==="—"?"":value(address.line2)}<br/>{value(address.city)}, {value(address.state)} {value(address.postal_code)}<br/>{value(address.country)}</p></div></div></aside></div></>}
