"use client";
import { FormEvent,useState } from "react";
export default function AdminSettingsForm(){const[message,setMessage]=useState("");const[error,setError]=useState("");const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setMessage("");setError("");const form=new FormData(event.currentTarget);const response=await fetch("/api/admin/settings",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({announcementEnabled:form.get("announcementEnabled")==="on",announcementMessage:form.get("announcementMessage"),announcementLink:form.get("announcementLink"),freeShippingThreshold:Number(form.get("freeShippingThreshold")),shippingCharge:Number(form.get("shippingCharge")),codEnabled:form.get("codEnabled")==="on",maintenanceMode:form.get("maintenanceMode")==="on"})});const data=await response.json();if(response.ok)setMessage("Store settings saved.");else setError(data.error)};return <form className="admin-form" onSubmit={submit}><div className="admin-panel" style={{padding:20,margin:0}}><h2>Announcement</h2><div className="admin-form-grid" style={{marginTop:18}}><div className="admin-field full"><label>Message</label><input name="announcementMessage" maxLength={160} placeholder="Free shipping on orders above ₹2,999"/></div><div className="admin-field full"><label>Optional link</label><input name="announcementLink" placeholder="/shop"/></div></div><div className="admin-checks" style={{marginTop:15}}><label><input name="announcementEnabled" type="checkbox"/> Show announcement</label></div></div><div className="admin-panel" style={{padding:20,margin:0}}><h2>Checkout and delivery</h2><div className="admin-form-grid" style={{marginTop:18}}><div className="admin-field"><label>Free shipping threshold</label><input name="freeShippingThreshold" type="number" defaultValue="2999"/></div><div className="admin-field"><label>Shipping charge</label><input name="shippingCharge" type="number" defaultValue="149"/></div></div><div className="admin-checks" style={{marginTop:15}}><label><input name="codEnabled" type="checkbox" defaultChecked/> Cash on delivery</label><label><input name="maintenanceMode" type="checkbox"/> Maintenance mode</label><label><input type="checkbox" disabled/> Razorpay (configure later)</label></div></div>{message&&<div className="admin-notice">{message}</div>}{error&&<div className="admin-error">{error}</div>}<button className="admin-button">Save settings</button></form>}
