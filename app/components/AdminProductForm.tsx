"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
export default function AdminProductForm(){const[image,setImage]=useState("");const[busy,setBusy]=useState(false);const[error,setError]=useState("");const router=useRouter();const upload=async(file:File)=>{const form=new FormData();form.set("file",file);const response=await fetch("/api/admin/upload",{method:"POST",body:form});const data=await response.json();if(!response.ok)throw new Error(data.error);setImage(data.url)};const submit=async(event:FormEvent<HTMLFormElement>)=>{event.preventDefault();setBusy(true);setError("");const form=new FormData(event.currentTarget);const body={name:form.get("name"),slug:form.get("slug"),sku:form.get("sku"),shortDescription:form.get("shortDescription"),description:form.get("description"),heroImageUrl:image||form.get("heroImageUrl"),priceInr:Number(form.get("priceInr")),compareAtPriceInr:form.get("compareAtPriceInr")?Number(form.get("compareAtPriceInr")):undefined,status:form.get("status"),isBestSeller:form.get("isBestSeller")==="on",isNewArrival:form.get("isNewArrival")==="on",isFeatured:form.get("isFeatured")==="on",sizes:String(form.get("sizes")||"").split(",").map(x=>x.trim()).filter(Boolean),inventoryQuantity:Number(form.get("inventoryQuantity")||0)};const response=await fetch("/api/admin/products",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body)});const data=await response.json();setBusy(false);if(!response.ok)return setError(data.error);router.push("/admin/products");router.refresh()};return <form className="admin-form" onSubmit={submit}><div className="admin-form-grid"><div className="admin-field"><label>Product name</label><input name="name" required/></div><div className="admin-field"><label>URL slug</label><input name="slug" required pattern="[a-z0-9-]+"/></div><div className="admin-field"><label>Base SKU</label><input name="sku" required/></div><div className="admin-field"><label>Status</label><select name="status" defaultValue="draft"><option>draft</option><option>active</option><option>archived</option></select></div><div className="admin-field full"><label>Short description</label><input name="shortDescription"/></div><div className="admin-field full"><label>Full description</label><textarea name="description"/></div><div className="admin-field"><label>Price (INR)</label><input name="priceInr" type="number" min="1" required/></div><div className="admin-field"><label>Compare-at price</label><input name="compareAtPriceInr" type="number" min="1"/></div><div className="admin-field"><label>Sizes (comma separated)</label><input name="sizes" placeholder="XS, S, M, L, XL"/></div><div className="admin-field"><label>Inventory per size</label><input name="inventoryQuantity" type="number" min="0" defaultValue="0"/></div><div className="admin-field full"><label>Hero image URL</label><input name="heroImageUrl" value={image} onChange={event=>setImage(event.target.value)} required/></div><div className="admin-field full"><label>Or upload hero image</label><input type="file" accept="image/*" onChange={async event=>{const file=event.target.files?.[0];if(file)try{setBusy(true);await upload(file)}catch(e){setError(e instanceof Error?e.message:"Upload failed")}finally{setBusy(false)}}}/></div></div><div className="admin-checks"><label><input name="isBestSeller" type="checkbox"/> Best Seller</label><label><input name="isNewArrival" type="checkbox"/> New Arrival</label><label><input name="isFeatured" type="checkbox"/> Featured</label></div>{error&&<div className="admin-error">{error}</div>}<button className="admin-button" disabled={busy}>{busy?"Saving…":"Save product"}</button></form>}
