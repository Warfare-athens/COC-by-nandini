"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export default function AdminOrderControl({id,current}:{id:string;current:string}){const[status,setStatus]=useState(current);const[busy,setBusy]=useState(false);const router=useRouter();return <div style={{display:"flex",gap:7,alignItems:"center"}}><select value={status} onChange={event=>setStatus(event.target.value)} style={{border:"1px solid #e8cdbc",borderRadius:999,padding:"7px 9px",background:"white",fontSize:10}}>{["unfulfilled","processing","packed","shipped","out_for_delivery","delivered","cancelled","returned"].map(value=><option key={value}>{value}</option>)}</select><button disabled={busy} onClick={async()=>{setBusy(true);await fetch(`/api/admin/orders/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:status==="cancelled"?"cancelled":"confirmed",fulfillmentStatus:status})});setBusy(false);router.refresh()}} style={{borderRadius:999,background:"#bb7068",color:"white",padding:"8px 11px",fontSize:9}}>{busy?"…":"Save"}</button></div>}
